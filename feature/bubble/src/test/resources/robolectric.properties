# Pin Robolectric-run unit tests (OfferGaugeBitmapTest) to the plain android.app.Application —
# this library module ships no @HiltAndroidApp entry point, so the default is correct and this
# only makes the intent explicit (mirrors :app's own pin).
application=android.app.Application
