package cloud.trotter.dashbuddy.domain.model.location

/**
 * A pure Kotlin representation of the user's LAT LON coordinates.
 */
data class Coordinates(
    val latitude: Double,
    val longitude: Double,
)