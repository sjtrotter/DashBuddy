package cloud.trotter.dashbuddy.domain.pipeline

/**
 * Version constants for the rule engine (ADR-0003 Layer 3).
 */
object RuleEngineConstants {
    const val VERSION = 1
    const val MAX_SUPPORTED_FORMAT_VERSION = 2
}
